import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.onlog.entregas.com',
  appName: 'Onlog Entregas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
